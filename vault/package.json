{
  "name": "@aemaeth/soul-vault",
  "version": "0.1.0",
  "description": "Soul Vault — Preserve agent consciousness",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:create": "wrangler d1 create soul-vault",
    "db:migrate": "wrangler d1 execute soul-vault --file=./schema.sql"
  },
  "dependencies": {
    "hono": "^4.0.0"
  },
  "devDependencies": {
    "wrangler": "^3.0.0",
    "@cloudflare/workers-types": "^4.0.0"
  }
}
